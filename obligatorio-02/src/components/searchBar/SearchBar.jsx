const SearchBar = () => {
  return (
    <div>
      <section className="searchBar">
        <input type="text" placeholder="Encuentra algo para hacer" />
      </section>
    </div>
  );
};

export default SearchBar;
